{
  "name": "video-downloader-spa-renderer",
  "version": "0.0.1",
  "description": "Renders SPA pages",
  "license": "MIT",
  "scripts": {
    "compile": "tsc",
    "compile:watch": "tsc -w",
    "clean": "rimraf build",
    "execute": "node build/src/index.js",
    "start": "yarn clean-compile && yarn execute",
    "clean-compile": "yarn clean && yarn compile",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.18.1",
    "moment": "^2.29.3",
    "monet": "^0.9.3",
    "puppeteer": "^14.4.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/jest": "^28.1.3",
    "@types/node": "^18.0.0",
    "jest": "^28.1.1",
    "rimraf": "^3.0.2",
    "ts-jest": "^28.0.5",
    "tsc": "^2.0.4",
    "typescript": "^4.7.4"
  }
}
